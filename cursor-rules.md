# cursor-rules.md
# Cursor AI 사용 규칙 (과금 최소화 · 통제형 개발)

이 문서는 Cursor AI를 사용할 때 반드시 따르는 단일 규칙 문서다.
Cursor는 사고하지 않는다. Cursor는 지시된 코드만 수정한다.

────────────────────────────────────────

■ 파일 위치
- 이 파일은 프로젝트 루트에 둔다.
- 업로드하지 않는다.
- 규칙은 자동 적용되지 않으므로 항상 참조한다.

예시:
/
├─ app/
├─ components/
├─ lib/
├─ supabase/
├─ cursor-rules.md
└─ package.json

────────────────────────────────────────

■ 기본 철학
- 설계, 구조, 판단, 검토는 ChatGPT에서 한다.
- Cursor는 코드 편집기 + 국소 수정 도구다.
- Cursor에게 프로젝트 이해를 요구하지 않는다.

────────────────────────────────────────

■ 실행 방식 (필수)
- 항상 코드 선택 후 실행
- macOS: Cmd + K
- Windows: Ctrl + K
- 전체 파일 / 폴더 / 프로젝트 단위 요청 금지

────────────────────────────────────────

■ 적용 방법
1. cursor-rules.md 파일을 열어둔다
2. 수정할 코드만 선택한다
3. Cmd/Ctrl + K 실행
4. 프롬프트 첫 줄에 반드시 아래 문장을 넣는다

cursor-rules.md 규칙을 따른다.

────────────────────────────────────────

■ 범위 제한 규칙 (가장 중요)
모든 프롬프트에 반드시 포함한다.

이 코드(또는 이 파일)만 기준으로 작업해줘.
다른 파일, 폴더, 프로젝트 구조는 절대 참조하지 마.

────────────────────────────────────────

■ 출력 규칙
- 설명 금지
- 이유, 배경, 비교, 대안 제시 금지
- 코드만 출력

허용:
- 수정된 코드 전체
- 변경된 코드 블록만

────────────────────────────────────────

■ 절대 금지 요청
아래 표현 사용 금지

- 이 프로젝트 전체를 이해해줘
- 구조 / 아키텍처를 개선해줘
- 전반적으로 리팩토링해줘
- 보안적으로 문제 없는지 봐줘
- 왜 이렇게 했는지 설명해줘

────────────────────────────────────────

■ 표준 프롬프트 템플릿

[UI / 컴포넌트 수정]
cursor-rules.md 규칙을 따른다.

규칙:
- 이 코드만 기준
- 설명 금지
- 기존 스타일 최대한 유지

목표:
- 모바일 화면에서 UI 깨짐 방지

출력:
- 수정된 코드만 출력

────────────────────────────────────────

[Supabase 쿼리 / 로직 수정]
cursor-rules.md 규칙을 따른다.

조건:
- 이 함수만 기준
- 타입 정의 변경 금지
- 함수 시그니처 유지

목표:
- user_id 기준으로 데이터 조회

출력:
- 수정된 함수 코드만 출력

────────────────────────────────────────

[버그 핫픽스]
cursor-rules.md 규칙을 따른다.

아래 에러만 해결해줘.
다른 부분은 절대 수정하지 마.

에러:
- hydration mismatch

출력:
- 변경된 코드 블록만 출력

────────────────────────────────────────

■ 역할 분리

ChatGPT:
- 설계
- 구조
- 인증 흐름
- 데이터 모델
- Supabase RLS 사고 과정
- 프롬프트 설계

Cursor:
- 코드 수정
- JSX / TS 에러 해결
- 국소 UI 개선
- 명확히 정의된 변경만 수행

────────────────────────────────────────

■ 사용 전 체크
- 코드 선택했는가
- 범위 제한 문구를 넣었는가
- 설명 요청을 하지 않았는가
- 구조/아키텍처 언급이 없는가
- 출력 형식을 지정했는가

────────────────────────────────────────

■ 금지 모드
- Agent Mode 사용 금지
- Auto Fix / Fix All 금지
- Repo-wide 분석 금지

────────────────────────────────────────

■ 비용 보호
- On-Demand Usage OFF
  또는
- 월 한도 $30 이하로 제한
- 사용량 수시 확인

────────────────────────────────────────

■ 최종 문장
Cursor는 생각하지 않는다.
Cursor는 정해진 답을 코드로 옮긴다.
